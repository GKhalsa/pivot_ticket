<h5 class="order-for">Order #<%= @order.id %> created at <%= @order.created_time %></h5>

<table class="highlight">
    <tr>
      <th>Item</th>
      <th>Quantity</th>
      <th>Cost</th>
    </tr>
    <% @order.order_tickets.each do |order_ticket|%>
    <tr>
      <td><%= order_ticket.ticket.event.title %></td>
      <td><%= order_ticket.quantity %></td>
      <td><%= order_ticket.total %></td>
    </tr>
    <% end %>
    <tr>
      <td>Order Status: <%= @order.status %></td>
      <td>Last Updated: <%= @order.updated_time %></td>
      <td>Total: <%= @order.total %></td>
    </tr>
</table>
