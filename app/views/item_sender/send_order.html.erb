<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      table {
        border-width: 0;
        border-style: none;
        border-color: #0000ff;
        border-collapse: collapse;
      }
      h1 {
        color: red;
      }
      td {
        border-left: solid 10px #000;
        padding: 0;
        margin: 0;
        width: 0px;
        height: 10px;
      }

      td.black { border-color: #000; }
      td.white { border-color: #fff; }
    </style>
  </head>
  <body>
    <h1>Hi <%= @user.name %></h1>
    <h3>Thank You for Your Purchase!</h3>
    <h3>Here are codes for your tickets:</h3>
    <% @tickets.each.with_index do |ticket, i| %>
          <p><%= "For #{ticket.seat_location}:" %></p><br/>
        <%= image_tag attachments["qr#{i}.png"].url %><br/>
      <% end %>
    <h2>Have a great day!</h2>
    <h2>Your order id: <%= @order.id %></h2>
  </body>
</html>
