<div id="venue-image"><%= image_tag @venue.image %></div>

<div class="row">
  <div id="venue-options" class="col s2 m2 l4">
    <div  class="collection">
      <div class="collection-item center"> <%= link_to "Add Event", new_admin_event_path %> </div>
      <a href="#!" class="collection-item center">Edit Venue</a>
      <a href="#!" class="collection-item center">Add Admin</a>
      <a href="#!" class="collection-item center">Delete Admin</a>
    </div>
  </div>
</div>

<ul class="collapsible" data-collapsible="accordion">
  <% @events.each do |event| %>
    <li>
      <div class="collapsible-header"><i class="material-icons"></i><%= event.title %></div>
      <div class="collapsible-body">  <%= link_to "hello", class: "waves-effect waves-light btn" do %>
                                      <i class="material-icons left">delete</i>Delete
                                      <% end %>

                                      <%= link_to "hello", class: "waves-effect waves-light btn" do %>
                                      <i class="material-icons left">mode_edit</i>Edit
                                      <% end %>

      </div>

    </li>
    <% end %>
  </ul>
